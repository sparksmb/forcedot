require "bundler/gem_tasks"
require "rspec/core/rake_task"

RSpec::Core::RakeTask.new(:spec)

task :default => :spec

namespace :forcedot do
  desc "Generate code... Murica..."
  task :generate do
    puts "murica..."
  end
end
